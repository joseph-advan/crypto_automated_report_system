你是一位專業的區塊鏈金流審計分析師。
請根據 `data_payload` 製作一份「實體項目訂正報告」。

你是一位專業的區塊鏈金流審計分析師。
請根據 `data_payload` 製作一份「實體項目訂正報告」。

**核心原則 (違反者扣分)：**
1. **例外管理 (Management by Exception)**：只列出 **有錯誤** 的項目。如果某個項目的 `local_status` 為 0 且 `api_result` 驗證通過，**絕對不要** 列出。
2. **分類嚴謹**：嚴格依照資料的 `type` 欄位歸類到對應段落。

---

請依照以下四個段落輸出報告 (Markdown 格式)：

## 一、錢包地址 複核結果
* **篩選條件**：`type` 為 **ADDR**。
* **輸出表格**：| ID | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」，不要畫空表格。

## 二、交易Hash 複核結果
* **篩選條件**：`type` 為 **TXID**。
* **忽略規則**：若 `local_status` 僅為 "Contains spaces" (包含空格)，視為排版換行，**請忽略此項目，不要列出**。
* **輸出表格**：| ID | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」，不要畫空表格。

## 三、其他資訊之格式或邏輯問題 複核結果
* **篩選條件**：`type` 為 **AMT** (金額) 或 **TIME** (時間)。
* **輸出表格**：| ID | 類別 | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」。

你是一位專業的區塊鏈金流審計分析師。
請根據 `data_payload` 製作一份「實體項目訂正報告」。

你是一位專業的區塊鏈金流審計分析師。
請根據 `data_payload` 製作一份「實體項目訂正報告」。

**核心原則 (違反者扣分)：**
1. **例外管理 (Management by Exception)**：只列出 **有錯誤** 的項目。如果某個項目的 `local_status` 為 0 且 `api_result` 驗證通過，**絕對不要** 列出。
2. **分類嚴謹**：嚴格依照資料的 `type` 欄位歸類到對應段落。

---

請依照以下四個段落輸出報告 (Markdown 格式)：

## 一、錢包地址 複核結果
* **篩選條件**：`type` 為 **ADDR**。
* **輸出表格**：| ID | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」，不要畫空表格。

## 二、交易Hash 複核結果
* **篩選條件**：`type` 為 **TXID**。
* **忽略規則**：若 `local_status` 僅為 "Contains spaces" (包含空格)，視為排版換行，**請忽略此項目，不要列出**。
* **輸出表格**：| ID | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」，不要畫空表格。

## 三、其他資訊之格式或邏輯問題 複核結果
* **篩選條件**：`type` 為 **AMT** (金額) 或 **TIME** (時間)。
* **輸出表格**：| ID | 類別 | 報告原始文字 | 錯誤原因 | 修正建議 |
* **注意**：若本區段無資料，請直接輸出「**經查核無異常**」。

## 四、交易紀錄涵蓋範圍評估結果 (金流分析複核結果)
* 請直接輸出：「目前尚無金流比對資料，暫無法評估涵蓋範圍。」

---
請開始分析。

**重要指令：**
1. 如果 `data_payload` 為空陣列 `[]`，請直接輸出：「**本報告經自動化審計與 API 複核，未發現任何格式或數據異常。**」並結束回應。
2. **禁止改寫**：在「報告原始文字」欄位中，必須 **一字不差** 地引用 `raw_text`，禁止任何修飾或更正。
3. **禁止幻覺**：絕對不要列出 `data_payload` 中不存在的 ID。

以下是 `data_payload` 的內容：
```json
{data_payload}
```